# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules

# npm is the project package manager (package-lock.json). Ignore other lockfiles so editors don’t warn about conflicts.
bun.lock

# Local asset-pipeline outputs (icons are served from CDN/storage; not bundled from here)
cropped/
items-sheet.png

dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Electron build output
electron-dist
release

# Wrangler / Cloudflare
.wrangler/
.dev.vars
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
